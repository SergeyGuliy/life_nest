export enum UserRole {
  ADMIN = 'ADMIN',
  GUEST = 'GUEST',
}

export enum RoomTypes {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE',
}

export enum MessageReceiverTypes {
  GLOBAL = 'GLOBAL',
  ROOM = 'ROOM',
  PRIVATE = 'PRIVATE',
}
